#include <iostream> 
#include <string> 
using namespace std; 
int main() 
{ 
  int a, b, c; 
  cout << "Enter variable values \n"; 
  cin >> a >> b >> c; 
  if ((a + b) > c) && ((a + c) > b) && ((c + b) > a) 
    cout << "Triangle exists" ; 
  else 
    cout << "Triangle does not exist" ; 
}
