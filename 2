#include <iostream> 
#include <string> 
using namespace std; 
int main() 
{ 
  int A, B, C; 
  cout << "Enter A, B and C"; 
  cin >> A >> B >> C; 
  if (A < B < C) 
    cout << "Inequality is right" ; 
  else 
    cout << "Inequality is wrong"; 
}
